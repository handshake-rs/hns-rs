# Benchmarks

Release qualification tracks header hashing, transaction parsing, block
parsing, Urkel verification, script execution, and experimental envelope
throughput. Stable benchmark baselines will be recorded here only after the
cross-project fixture corpus and target hardware profile are pinned; no
synthetic performance number is represented as a baseline yet.
